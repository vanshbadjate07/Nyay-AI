# ===================================
# NyayAI Environment Configuration
# ===================================

# Google Gemini API Key (REQUIRED)
# Get your API key from: https://makersuite.google.com/app/apikey
# Without this, the application will not start
GOOGLE_API_KEY=your_gemini_api_key_here

# Gemini Model (Optional)
# Default: gemini-2.0-flash-exp
# Other options: gemini-1.5-flash, gemini-1.5-pro
GEMINI_MODEL=gemini-2.0-flash-exp

# Upload Directory (Optional)
# Default: .uploads
# Where uploaded files are temporarily stored
UPLOAD_DIR=.uploads

# Max File Size in Bytes (Optional)
# Default: 15728640 (15 MB)
# Maximum size for uploaded documents
MAX_FILE_BYTES=15728640

# CORS Origins (Optional)
# Default: * (allow all origins)
# For production, specify allowed domains: http://yourdomain.com
CORS_ORIGINS=*
